# Turing-Machine-String-Reversal-Simulator
This project implements a **Turing Machine** that reverses strings composed of 'a' and 'b' characters. The simulator provides a visual representation of the Turing Machine's tape, head position, state transitions, and step-by-step execution.
